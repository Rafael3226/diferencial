import 'package:flutter/material.dart';

class Continuidad extends StatelessWidget {

  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      title: Text('Continuidad'),
      elevation: 0.0,
    ),
      body: Text("5"),
    );
  }
}